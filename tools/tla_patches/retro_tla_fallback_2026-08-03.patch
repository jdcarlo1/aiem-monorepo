diff --git a/tools/check_protected_push.py b/tmp/check_protected_push_retro.py
index dab82e8..06ba3bf 100644
--- a/tools/check_protected_push.py
+++ b/tmp/check_protected_push_retro.py
@@ -33,6 +33,7 @@ a BYPASS entry.
 from __future__ import annotations
 
 import fnmatch
+import hashlib
 import json
 import os
 import re
@@ -101,6 +102,15 @@ def _load_approvals() -> list[dict]:
     return records
 
 
+
+def _compute_diff_sha(sha: str, protected_files: list[str]) -> str:
+    """Compute sha256 of the raw unified diff for the commit's protected files."""
+    rc, out, _ = _run(["git", "show", sha, "--"] + protected_files)
+    if rc != 0:
+        return ""
+    return hashlib.sha256(out.encode()).hexdigest()
+
+
 # ── Core gate ─────────────────────────────────────────────────────────────────
 
 def check_range(remote_sha: str, local_sha: str) -> tuple[bool, str, str]:
@@ -151,6 +161,20 @@ def check_range(remote_sha: str, local_sha: str) -> tuple[bool, str, str]:
 
         match = _TLA_RE.search(msg)
         if not match:
+            _retro = next(
+                (r for r in approvals
+                 if r.get("retroactive_for_commit", "").lower() == sha.lower()
+                 and r.get("used") is True
+                 and r.get("self_issued") is False
+                 and r.get("diff_sha", "") == _compute_diff_sha(sha, protected)),
+                None,
+            )
+            if _retro:
+                print(
+                    f"[pre-push-gate] {sha[:12]}: protected={protected} "
+                    f"retroactive={_retro.get('approval_id', '?')} diff_sha=VERIFIED — PASS"
+                )
+                continue
             plist = ", ".join(protected)
             return (
                 False,
