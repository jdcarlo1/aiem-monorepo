# Directive — Complete Function Check + Column Approval (2026-07-31)

## Decision 1 — NOT approved yet
Do not conclude the 7 prod-only functions are safe to drop or migrate. Your own 39-row `information_schema.routines` query is confirmed incomplete (missed `aiem_deletion_guard_stmt`, `trg_fn_oe_decision_audit_immutable`, etc.).

Required before this decision is revisited:
1. Run a complete dev function inventory — query `pg_proc` joined to `pg_namespace` (not `information_schema.routines`) for the dev schema, or `\df+` equivalent, capturing ALL functions including trigger functions.
2. Cross-check each of the 7 prod-only functions (`d3gack_block_mutation`, `d3gd_block_mutation`, `d3gel_block_mutation`, `d3gr_block_mutation`, `_oe_dpl_guard_immutability`, `trg_fn_correction_ledger_immutable`, `trg_fn_legacy_exceptions_immutable`) against that complete list.
3. Paste the raw query + full result set — no row count summaries, no "confirmed missing" narrative without the query output.
4. State plainly whether each of the 7 exists in dev or not, per function, based on the raw output.

## Decision 2 — Approved
`ALTER TABLE polygon_market_daily ADD COLUMN captured_at TIMESTAMPTZ` on production is approved. Proceed.

Required evidence for this item:
- Raw `ALTER TABLE` execution output
- sha256 before/after for any migration file changed
- Confirmation via raw query that the column now exists in production with correct type/nullability

## Standing checklist (applies to both items)
- Raw execution output only — no paraphrased "confirmed"/"exists"/"missing" claims
- Data Immutability Rule: no DROP/ALTER/DELETE beyond what's explicitly approved above
- sha256 cross-check any changed script against current canonical: tools/verified_run.sh = dce94f6e19dfc5c7952ab9eee7015b7eb10c3ff1e0ca60263279658ab166f826, tools/verify_chain.sh = 4804b54704634c490d4d7140e88cc4e9874058292b6879d9dbdeb3e86cdd7e12
- If evidence can't be produced, say so — no padding
- "PASS" only if fully evidenced end to end; otherwise label as "partial" or "cleared to proceed"
