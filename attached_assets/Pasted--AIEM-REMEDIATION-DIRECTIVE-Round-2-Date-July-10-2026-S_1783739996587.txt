# AIEM REMEDIATION DIRECTIVE — Round 2
**Date:** July 10, 2026
**Status:** MANDATORY — same nonce/hash protocol as before applies to every claim.
**New nonce for this round:** `AIEM-20260710-JR-9d2e4b`

The last verification pass was real — nonces matched, hashes checked out, raw SQL was pasted. Good. That earns the next round of trust, not a free pass. Same rules: every claim needs raw output wrapped in the nonce, hashed, and pasted. No narrative-only claims.

This round is about **fixing**, not just documenting, the three concrete problems the last audit surfaced.

---

## FIX 1 — Stale spec docs (2-stage discrepancy)

**Problem confirmed:** `aiem_registry.py` (line 439) and `aiem_diagram2_trace_audit.py` (line 4) both still say "21 canonical / 21-stage," while `main.py` actually calls `execute_stage()` for stages 20, 21, 22, AND 23. The spec is wrong and has been wrong for at least a day.

**Required fix:**
- Update both files so the stage count and any stage-name lists correctly reflect all 23 stages, including 22 (`feedback_loop`) and 23 (`memory`).
- Paste a diff (before/after) for each file, wrapped in the nonce.
- Paste a fresh file-modified timestamp for both files, proving the edit actually landed on disk.

**Rejection criteria:** If the timestamp on either file is unchanged from what was already reported (`aiem_registry.py`: 2026-07-09 19:15:39; `aiem_diagram2_trace_audit.py`: 2026-07-10 12:15:00), the fix did not happen — reject and re-run.

---

## FIX 2 — Explain (and fix if broken) why hypotheses never reach `validated` status

**Problem confirmed:** Source 9 (`washout_ignition`, id=9) doesn't exist at all in `aiem_signal_discoveries`. Source 10 (`squeeze_reversion`, id=4) exists but is stuck at `status='hypothesis'`. Meanwhile id=1 and id=2 show real `oos_edge` values (0.086 and 0.048) but `p_value=None` — meaning some out-of-sample statistical test ran, but nothing ran the significance test or the promotion step that would move status to `validated`.

This is the actual bug, not just a "waiting on data" situation. A hypothesis with a computed edge but no p-value and no path to `validated` is a pipeline that stops partway and nothing downstream ever notices.

**Required fix — trace and answer, in order:**
1. Is there code anywhere that is *supposed* to compute a p-value and promote a row to `validated`? File + line citation.
2. If yes: why hasn't it run for id=1 or id=2, which already have `oos_edge` computed? Show the raw trigger condition it's waiting on.
3. If no such code exists: state that plainly — this means `validated` is currently an unreachable status for any hypothesis, which means Source 9 and Source 10 can NEVER fire under current code, not just "haven't fired yet." That is a materially different finding than what was reported, and needs to be said in those words if true.
4. Do not write a promotion query or manually flip `status='validated'` on id=4 or a new id=9 row without telling me first — I don't want a manufactured pass condition. Show me the real mechanism or the real absence of one.

**Rejection criteria:** Any answer that skips straight to "I'll add a script to promote these" without first answering questions 1-3 is rejected. I need to understand whether this is a wiring gap or a designed gate before anything gets changed.

---

## FIX 3 — Confirm Source 11's data dependency is actually reliable

**Problem confirmed:** Source 11 depends on `polygon_market_daily` being populated by the 8:35 AM job, but that job's reliability hasn't been verified — only assumed.

**Required fix:**
- Raw query showing the last 5 rows of whatever job-log table tracks the 8:35 AM market-data job, with timestamps and success/failure status.
- If no such log table exists, say so — that itself is a gap (no way to know if the job silently failed on a given day).

**Rejection criteria:** "The job usually runs fine" is not evidence. I need row-level proof of recent runs or an explicit statement that no such tracking exists.

---

## FINAL REQUIREMENT (same as before)

End with: **"Every claim above is backed by output pasted in this response, generated during this session, verified against nonce `AIEM-20260710-JR-9d2e4b`."**

If any part cannot be truthfully verified, say exactly what's missing instead of rounding up.
