# Directive: Real Neon DDL — Replace Inferred Stubs — 2026-08-04

Cursor pulled real DDL for all 4 remaining items directly from Neon prod (`neondb @ ep-spring-flower-aqxm8amx.c-8.us-east-1.aws.neon.tech`). Replace the inferred stubs in `dev_schema_bootstrap.sql` with these exact definitions — no more guessing.

## 1. `layer9_scores.xmom_zscore` (ADD COLUMN)
```sql
ALTER TABLE layer9_scores
  ADD COLUMN xmom_zscore DOUBLE PRECISION NULL;
```
(Confirmed: `data_type=double precision`, `udt_name=float8`, `is_nullable=YES`, `column_default=null`, `ordinal_position=21`.)

## 2. `ml_training_runs` (CREATE TABLE)
```sql
CREATE TABLE ml_training_runs (
  id BIGINT PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
  model_name TEXT NOT NULL,
  run_kind TEXT NOT NULL DEFAULT 'fit',
  started_at TIMESTAMPTZ NOT NULL DEFAULT now(),
  finished_at TIMESTAMPTZ NULL,
  n_train INTEGER NULL,
  n_val INTEGER NULL,
  train_loss NUMERIC NULL,
  val_loss NUMERIC NULL,
  val_auc NUMERIC NULL,
  val_accuracy NUMERIC NULL,
  metrics_json JSONB NULL,
  status TEXT NOT NULL DEFAULT 'completed',
  note TEXT NULL
);
-- Also confirm/add: INDEX ml_training_runs_model_started_idx (name suggests model_name, started_at — verify exact columns before creating if index DDL wasn't fully captured)
```

## 3. `intraday_continuation_models` (CREATE TABLE)
```sql
CREATE TABLE intraday_continuation_models (
  id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
  version INTEGER NOT NULL,
  model_blob BYTEA NOT NULL,
  feature_names JSONB NOT NULL,
  held_out_precision NUMERIC NULL,
  n_train INTEGER NULL,
  is_live BOOLEAN NOT NULL DEFAULT false,
  created_at TIMESTAMPTZ NOT NULL DEFAULT now(),
  note TEXT NULL
);
```

## 4. `reconciliation_log.mode` (ADD COLUMN)
```sql
ALTER TABLE reconciliation_log
  ADD COLUMN mode TEXT NULL DEFAULT 'paper';
```
(Confirmed: table has 7 columns total — `id, checked_at, only_in_broker, only_in_db, mismatch_found, resolved, mode`; `mode` is `ordinal_position=7`, `is_nullable=YES`, `column_default='paper'::text`.)

## What I need
1. Replace all 6 stub definitions in `dev_schema_bootstrap.sql` with the real DDL above (items 1 and 4 are ADD COLUMN, items 2 and 3 — plus the earlier `aiem_diagnostics`/`aiem_pipeline` — are CREATE TABLE).
2. Apply to dev DB (helium), verify via `information_schema` that types now match exactly.
3. Generate a fresh migration diff. Confirm explicitly it does NOT propose dropping or altering any of the 6 items, and paste the full diff for review.
4. Only after that confirmation — you're clear to Publish.

Double-check the `ml_training_runs_model_started_idx` index definition before creating it; the screenshot only showed the index name, not its column list — confirm the actual columns from Neon before writing that DDL, don't guess.
