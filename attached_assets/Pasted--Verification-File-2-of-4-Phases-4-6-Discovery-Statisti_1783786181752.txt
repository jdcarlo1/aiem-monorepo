# Verification File 2 of 4 — Phases 4–6 (Discovery → Statistical → Technical/Options)

## Scope

**Phase 4 — Discovery Engine:** Discovery Engine, Hypothesis Registry, Active Hypothesis Selection, Literature Scanner, Signal Drift Monitor, Thompson Sampling (research allocation)

**Phase 5 — Quantitative & Statistical Analysis:** GARCH, VPIN, Hurst Exponent, Bipower Jump, Variance Risk Premium, Risk Neutral Density, Statistical Arbitrage, Engle-Granger, Cointegration, PCA, Absorption Ratio, Fisher's Exact Test, Benjamini-Hochberg FDR, Walk-Forward Validation, Out-of-Sample Testing, Signal Decay, Monte Carlo, Bayesian Statistics, Regime Probability Models

**Phase 6 — Technical & Options Analysis:** VWAP, CTA Levels, Options Structure, Gamma Exposure, Put/Call Skew, Volume Profile, RSI, Momentum, Gap Analysis, Pullback, Breakout, Short Squeeze, Trend, Relative Strength

## Directive — paste to the agent verbatim

This is the largest batch — do it in three passes (Phase 4, then Phase 5, then Phase 6) rather than one combined dump, so each pass's output can be checked before moving to the next.

For every item listed above, produce a table with these exact columns. Every row must be backed by raw grep/ast-grep output pasted immediately above or below the row — no row may be filled from memory or summary.

| Item | Defined at (file:line) | Every call site (file:line) | Return value: assigned to a variable or discarded? | If assigned — what downstream function/module reads that variable, with exact line? If never read again, say DEAD-END. | Is this called from inside the live candidate pipeline (Diagram 2) or from a separate/orphaned path? |
|---|---|---|---|---|---|

For Phase 5 in particular, also answer per item: is this indicator's numeric output actually passed as an input argument into the Phase 8 scoring/synthesis function, or does it only get written to a log/DB table that scoring never reads? Show the scoring function's signature and the exact argument or dict key that carries this indicator's value. If you cannot find it in the signature or an inputs dict, say NOT WIRED TO SCORING and show what scoring's function signature actually contains instead.

For Phase 4, additionally confirm: does the Discovery Engine's output (the list/ranking of candidate tickers) actually become the input that Phase 5/6 iterate over? Show the exact variable handoff — the line where Phase 4 returns its candidate list and the line where Phase 5 or 6 consumes it as a loop/iteration source. If Phase 5/6 instead pull tickers from a separate hardcoded list or a different query, say DISCONNECTED and show both sources.

Do not write summary prose. Output only commands, raw output, and completed tables.

## What a passing result looks like

No Phase 5 indicator is "NOT WIRED TO SCORING" — a statistical engine that computes a number nobody uses in the final decision is exactly the write-only pattern already found in the Thompson sampler bug.

Phase 4's candidate list is confirmed as the actual iteration source for Phase 5/6, not a parallel, unconnected list.
