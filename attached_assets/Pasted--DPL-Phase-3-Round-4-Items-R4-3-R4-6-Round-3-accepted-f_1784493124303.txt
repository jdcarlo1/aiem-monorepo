# DPL Phase 3 — Round 4, Items R4.3–R4.6

Round 3 accepted for A1, F1, F2, F4, F5, F6, F7. R4.1/R4.2 issued separately — do not re-run or re-paste that evidence here.

## R4.3 — replay_decision() has no production call site
`grep -rn "replay_decision"` returned no output outside the definition and verifier. Drift detection runs only on manual verifier invocation, never in production.
- State plainly whether this was intended.
- Propose (do not implement) where replay_decision() should be invoked in the live path, and what happens on CODE_DRIFT: alert, halt, or log-only.
- Proposal only. No change to scheduler or pipeline without approval.

## R4.4 — provenance of the five is_test_record=FALSE rows
Raw query: decision_id, created_at, alert_id for all five, joined to oe_decision_audit. Full result set.
- For each: live scheduler decision, or backfill/manual insert. State which, per row, with the evidence that establishes it.
- Do not infer origin from created_at alone. If origin cannot be established from stored data, say so in one line.
- If any are backfill: state that Criterion 1 remains blocked and R6 stays open.

## R4.5 — permanently unreplayable rows
`ee74327806` (old source-only hash) and `64d956c7ee` (D12=0.99, mutation window).
- Confirm both rows are retained. Raw SELECT showing each exists.
- Raw output of the CODE_DRIFT cause for each.
- Do not state or propose how these should be counted against Criterion 1. That determination is mine.

## R4.6 — SEQ chain discontinuity (single scoped write)
SEQ ran 3 → 2 across Round 3. The counter is per-VM-lifetime and cannot order the historical record.
- Append — do not rewrite — one statement to that effect in the DPL README. Name the file before writing.
- sha256 before/after. Raw diff.
- This is the only write authorized under this directive.

## Standing (every reply)
- Raw command output only. No prose substitution. Raw `grep -n`/`sed -n` for any code-location claim.
- Raw SQL + full result set for any DB claim; >10 rows via `\copy`.
- sha256 before/after, changed files this session only.
- Wrapper cross-check: `tools/verified_run.sh` + `artifacts/stock-scanner-api/verify_chain.sh`, untruncated. verify_chain.sh canonical = `ca7896c7c832ef53430dfd07319418000d9139566c9e52720f587aa9c9840d1f`. verified_run.sh canonical = UNRESOLVED pending H0.
- Run evidence-producing commands through `verified_run.sh`; report SEQ for each.
- `git diff HEAD --stat` and `git status --porcelain` AFTER all work.
- No mutation of live production files beyond the R4.6 append. No deletions. No self-authorized restores.
- If a claim cannot be evidenced, say so in one line.
