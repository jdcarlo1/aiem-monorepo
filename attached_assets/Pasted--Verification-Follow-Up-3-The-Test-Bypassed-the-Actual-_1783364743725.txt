# Verification Follow-Up #3 — The Test Bypassed the Actual Pipeline

## What the last test actually proved vs. what it claimed

The `promote_ready` / `hypothesis_failing` tests you ran got real `tg_api_ok:true`
and `HTTP_STATUS:200` responses — that's genuine proof `_tg_send()` reaches the
Telegram API and works.

But the message payload itself says: **"no DB rows written — this is a
synthetic test only."** That means `_tg_send()` was called **directly** with
hand-typed text. Nothing was inserted into the DB, and nothing exercised
`_dc_module5_promotion_check()` reading `_m5_notable` or the Module 8 loop that
iterates over it and decides what to send.

**This proves the messaging pipe works. It does not prove the actual pipeline
works.** I need evidence that a real row landing in the DB gets picked up,
classified, and routed to Telegram through the real code path — not typed by
hand into `_tg_send()`.

---

## What I need instead

1. **Insert one real test row** into whatever table `_dc_module5_promotion_check()`
   actually queries (the same table that produces `_m5_notable`), with:
   - `promotion_status = 'promote_ready'`
   - Obvious test markers: `discovery_id = -1`, hypothesis text prefixed
     `[TEST — DELETE ME]`
   - Paste the raw `INSERT` statement and confirmation it succeeded.

2. **Trigger the actual scheduled/cyclical job** — the same one that runs
   `_dc_module5_promotion_check()` and feeds Module 8 in production. Do not
   call `_tg_send()` directly and do not hand-write the message text.

3. **Paste raw evidence that the real code path executed**:
   - The query `_dc_module5_promotion_check()` ran, showing it picked up the
     test row
   - The exact message content Module 8 generated from that row (should be
     built from the row's actual columns, not typed by a person)
   - The live Telegram API response for that message

4. **Repeat for `hypothesis_failing`** the same way — real row, real trigger,
   no shortcuts.

5. **Clean up afterward**: delete the test row(s) and paste a follow-up query
   confirming 0 rows remain.

## What counts as resolved

- Evidence the message content was *generated by the code*, not written by
  hand — e.g. it should reflect the exact stats/values from the row you
  inserted, in whatever format the code actually produces (which may differ
  slightly from the earlier synthetic version).
- Confirmation the trigger was the real scheduled job / cycle function, not a
  direct call to `_tg_send()`.
- If the real job can't be safely triggered on demand (e.g. it's tied to a
  cron schedule with no manual hook), say so explicitly and propose how to
  add a safe manual trigger — don't substitute another synthetic shortcut.
