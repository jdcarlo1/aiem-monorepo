Implement AEIM closed-loop learning and audit completion for EVERY stock alert.

This must run automatically every single time a stock is alerted to me.

TRIGGER TIME:
Run immediately after every stock alert is generated by the stock scanner, before the alert is considered complete.

Also run again after every paper trade closes.

GOAL:
Fix the remaining gaps #1 through #5 and make the AEIM learning loop fully traceable every time.

REQUIRED MODULES TO COMPLETE:

1. COMPLETE AUDIT TRACE ID FOR EVERY TRADE

Every stock alert must create a permanent audit_trace_id.

Requirements:
- audit_trace_id must never be NULL.
- Store audit_trace_id in aiem_paper_trades.
- Store audit_trace_id in all AEIM audit tables.
- Every step of the trade must be linked by the same trace_id.

If a stock alert creates a candidate, the system must log:

signal_received
candidate_intake
aiem_analysis_started
module_scores_generated
candidate_ranking_created
final_aiem_decision
paper_trade_created_or_rejected
outcome_recorded
learning_update_applied
future_decision_effect_checked

If any step is missing, mark the trace as FAIL.

2. STORE SIGNAL TRUST HISTORY PER UPDATE

Create or repair a table called signal_trust_history.

Every time AEIM updates trust for a signal source, store:

id
created_at
audit_trace_id
trade_id
ticker
signal_source
old_trust_score
new_trust_score
delta
reason_for_change
win_loss_result
pnl
pnl_pct
rolling_win_rate
n_trades_used
learning_module_source

Requirement:
No trust update is allowed unless the before/after value is stored.

3. FIX THOMPSON SAMPLER / TEMPLATE FEEDBACK

Repair dc_template_feedback and Thompson sampler logic.

Every signal template must track:

template_name
wins
losses
alpha
beta
sampled_score
last_updated
audit_trace_id
trade_id
ticker
signal_source
reward
penalty
reason

After every closed trade:
- update alpha/beta
- store the before/after values
- prove the sampler affects future template ranking

If Thompson sampler is not active, mark it PARTIAL, not PASS.

4. TRAIN PPO / RL POLICY OR MARK HONESTLY INCOMPLETE

The PPO / RL buffer must not just store rows.

After enough buffer rows exist, run the training step.

Store:

rl_training_run_id
started_at
completed_at
buffer_rows_used
policy_version_before
policy_version_after
loss_value
gradient_step_completed true/false
reward_mean
reward_std
notes

If no gradient step runs, the system must honestly report:
"PPO buffer exists but PPO policy is not trained yet."

Do not claim full RL learning unless a gradient update actually ran.

5. STORE INTERMEDIATE CANDIDATE RANKING

Every stock alert must store the full candidate ranking before the final pick.

Create or repair a table called aiem_candidate_rankings.

Store:

id
created_at
audit_trace_id
run_id
ticker
signal_source
candidate_rank
raw_score
module_score_json
trust_multiplier
drift_multiplier
rl_weight
final_adjusted_score
accepted_or_rejected
decision_reason
decision_authority

Requirement:
If AEIM suppresses a ticker or promotes another ticker, the DB must prove why.

EVERY ALERT WORKFLOW:

When my scanner alerts a stock:

Step 1:
Create audit_trace_id.

Step 2:
Log signal_received with:
ticker
signal_source
alert_time
scanner_score
scanner_reason

Step 3:
Run AEIM candidate intake.

Step 4:
Run AEIM module scoring.

Step 5:
Store intermediate candidate ranking.

Step 6:
Apply trust weights, drift gates, RL weights, Thompson sampler values, and learned penalties/boosts.

Step 7:
Store final_aiem_decision with:
decision_authority=AIEM
approved/rejected
confidence_score
reason
trace_id

Step 8:
If paper trade is opened, store audit_trace_id in aiem_paper_trades.

Step 9:
When trade closes, record:
entry_price
exit_price
pnl
pnl_pct
market_return
relative_performance
success_or_failure_reason

Step 10:
Apply learning update.

Step 11:
Store before/after values for:
signal trust
strategy weight
module weight
drift multiplier
Thompson sampler alpha/beta
RL buffer reward
candidate ranking impact

Step 12:
Check the next later trade where the updated values affected the decision.

Step 13:
Generate a report for every completed trade:

AEIM_TRADE_LEARNING_AUDIT_REPORT

The report must include:

1. Full audit_trace_id
2. Original stock scanner alert
3. AEIM intake decision
4. Intermediate candidate ranking
5. Final AEIM decision
6. Paper trade result
7. Outcome grading
8. Failure/success reason
9. Learning update
10. Before/after parameter change
11. Later decision affected by that update
12. PASS / PARTIAL / FAIL verdict

STRICT RULES:

- Use live database data only.
- No mock data.
- No fake examples.
- No hard-coded proof.
- If a table is empty, say it is empty.
- If a module is incomplete, say it is incomplete.
- Do not claim AEIM learned unless future decisions actually changed.
- Do not claim full autonomy unless the audit trace proves it.
- Every stock alert must go through this process automatically.

FINAL OUTPUT REQUIRED:

After implementation, produce:

1. SQL migration file
2. Updated Python code
3. Scheduler/trigger wiring
4. One completed sample audit report
5. PASS / PARTIAL / FAIL status for each of the five gaps

This should run automatically:
- immediately after every scanner alert
- again after every paper trade closes
- every trading day during the normal alert/paper-trading cycle