# Options Engine — Code-Level Findings (options_engine_all_6.txt)

## Confirms prior known facts (raw grep/line evidence)
- `ready_for_decision` gate: line 10965-10966 — `raise ValueError("not ready_for_decision: ...")`.
- `[P2_INIT]` log: line 10125. `[P2_GATE]` log: line 11474. `[P2_CAPTURE]` log: line 11493/11512.

## Correction to diagnostic framework
`[P2_GATE]` (11474) is **unreachable for NO_TRADE**. NO_TRADE hits an explicit early `return` at line 11311 — before line 11474 ever executes. So a real log will NEVER show `[P2_GATE] ... direction=NO_TRADE`. Don't grep for that combination; it can't exist.

## Actual 3-way signature to grep for, per ticker/date
1. **Legit NO_TRADE** (scoring gate correctly rejected): `[P2_INIT]` present, then line ~11103 —
   `[exec] job_id=... {ticker} → NO_TRADE call=... put=... margin=...`
   No `[P2_GATE]` line — that absence is expected, not a bug.
2. **ValueError before scoring** (data-readiness): `[P2_INIT]` present, then line ~11801 —
   `[exec] FAILED job_id=... ticker=...: not ready_for_decision: <verdict>`
   `options_pipeline_jobs.status` set to `FAILED` (or `FAILED_GATE` if it failed after `_gate_fired`).
3. **Scheduler never fired**: no `[exec] START job_id=...` line at all for that ticker/date (line 10023).

## Item 3 — scoring/threshold logic (lines 10968-11006)
- `call_score`, `put_score` = `compute_req6_score()` output (12-dimension REQ6 scorer, weights in `_REQ6_SCORING_WEIGHTS`, ~line 7839).
- `margin = abs(call_score - put_score)`.
- Decision:
  - `call_score >= put_score and call_score >= 55 and margin >= 10` → `LONG_CALL` (tie goes to CALL)
  - `put_score > call_score and put_score >= 55 and margin >= 10` → `LONG_PUT`
  - else → `NO_TRADE`

## Still outstanding — cannot be resolved from source alone
The actual raw scheduler log from the next live 9:45 AM ET run. I have no production/live log access — this still has to come from Joel/the agent, grepped for `[exec]`, `[P2_INIT]`, and the ticker(s) for that date, per the signature above.