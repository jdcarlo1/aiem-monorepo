# Directive — Resolve --full-test Memory Contention & Get Clean VERDICT PASS

## Context
`d3_negctl_harness.py --full-test` was killed (SIGKILL) during `main.py` import.
Root cause identified: the harness spawns a subprocess that imports the full
66K-line `main.py` a second time, while the live stock-api server already holds
those imports in memory. This is a memory contention / environment issue, not
a code defect. `--dry-run` already passed cleanly with correct isolation — this
directive only needs a clean `--full-test` run to close out VERIFIED SHADOW.

## Task

1. **Choose and justify an approach** to eliminate the memory contention. Options,
   in order of preference — pick the first one that's actually feasible in this
   environment and explain why:
   a. Temporarily pause/stop the live stock-api server for the duration of the
      full-test run only, then restart it immediately after. State how long the
      server was down.
   b. Run the full-test subprocess in a separate, higher-memory environment/container
      if one is available.
   c. Reduce the import footprint — e.g., a lighter import path that doesn't pull
      in the entire main.py, if the harness can be adapted to only import what's
      needed for the governance check. Only do this if it doesn't change what's
      being tested (must still exercise the real `_aiem_paper_execute_today` →
      `require_governance_authorization` path).
   Do NOT increase the container's memory limit or modify production infra
   config without flagging that as a separate decision for Joel.

2. **Run `d3_negctl_harness.py --full-test` fresh** using the chosen approach.
   - Paste the full raw terminal output, including the `[VERDICT]` line.
   - If it still does not produce `[VERDICT] PASS`, state exactly what happened
     — do not soften or omit a second failure.

3. **Confirm isolation held** (same as before): raw SQL row counts in
   `d3_governance_requests`, `d3_governance_decisions`, `d3_governance_event_links`,
   `d3_governance_acks` in both `d3_test_isolation` and `public` schemas,
   before and after. Confirm `public` and `aiem_paper_trades` show zero movement.

4. **If the live server was paused**, confirm it was successfully restarted and
   is healthy afterward (e.g., a basic health-check ping or log line showing it's
   back up). This matters — don't leave it down.

5. **State current stage explicitly.** Does a clean `[VERDICT] PASS` now close
   out VERIFIED SHADOW? If yes, say so plainly. If something else is still open,
   say what.
   Reminder: VERIFIED SHADOW closing does NOT mean PAPER ENFORCEMENT exists —
   that requires separate new integration work (D3 actually halting the paper
   Decision Engine), not implemented yet.

## Standing verification block (required for every claim above)
No task is complete based on description alone. For every claim, provide:
1. Raw terminal output of actual execution — including raw grep output for any
   claim about code locations, call chains, or whether a function/pattern exists.
   Never paraphrase what grep would show.
2. Exact before/after file diff (if applicable).
3. sha256 hash before/after for any changed files.
4. Raw SQL query + full result set for any database claim.
Narrative or paraphrased claims of "fixed," "tested," "found," or "confirmed"
without pasted raw evidence are rejected. State explicitly if you cannot produce
a required evidence item.

## Evidence chain protocol
Run all evidence-producing commands through `verified_run.sh` (hash-chained
append-only logging wrapper, sha256 72824bfe...24abd) instead of running them
directly. Validate the log with `verify_chain.sh` (sha256 7294abeb...1fbec7)
before declaring this directive complete, and paste that script's full output.

Note: the evidence chain proves internal log consistency only — not that
commands were genuinely executed against the real production DB. Joel may
spot-check by independently re-running a sampled command himself.

## Data immutability
Do not delete, truncate, drop, or overwrite any row, table, or file — including
test/synthetic data — without Joel's explicit prior approval in that session.
State what you want to delete and why, wait for approval, then log it.
