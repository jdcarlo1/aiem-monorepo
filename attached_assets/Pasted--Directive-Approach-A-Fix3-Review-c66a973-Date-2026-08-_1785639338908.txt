# Directive — Approach A / Fix3 Review (c66a973)
Date: 2026-08-02
STOP after each item and report. No autonomous-trading enabling. No threshold commits.

---

## ACCEPTED (do not redo)

- ITEM B Fix3: provenance corrected to `1fe78da`; Tradier override at 1663-1692 confirmed wired; July ~0.24 spread explained by Tradier failure. CLOSED.
- ITEM A math: 48/48 suite (scipy reference + finite-difference + mutation) accepted as evidence of correct BS delta inversion. Math is not in question below.

---

## ITEM 1 — _sinc breakpoints shipped without authorization

Your before/after shows `sinc=$1.00 now, was $2.50` (CLF) and `$2.50 now, was $5.00` (HAL). That is Fix1, reverted last session and reserved for Joel. It shipped inside an Item A commit scoped to strike selection.

Do exactly this:

1. `git show c66a973 -- artifacts/stock-scanner-api/aiem_options_scheduler.py | grep -n "_sinc" ` — paste raw output.
2. State the exact before and after breakpoint tables, line numbers included.
3. Do NOT revert yet. Report only.
4. State in one sentence why this was included without being flagged.

---

## ITEM 2 — Fix2 framing correction

You wrote that the 0.25 gate "was silently overwritten by a later commit." It was not silent. It was Joel's authorized revert of Fix2. Your Pending table lists "Fix2 revert" as an open action.

Do exactly this:

1. Remove "Fix2 revert" from your pending list.
2. Confirm in writing: `probability_estimate < 0.35` is the live gate and will not be changed by you under any justification.
3. Any future proposal to move it comes to Joel as a proposal only, with historical outcome data, never as a commit.

---

## ITEM 3 — 35-delta reference verification

Your citations do not hold as written. CBOE BXM writes at-the-money calls. CLL is a 95/110 collar. Neither states 35-delta as a minimum OTM benchmark.

Do exactly this:

1. For Natenberg §8: quote the exact sentence and page number supporting 35-delta as the directional/speculative boundary. If you cannot produce it, say so plainly.
2. For CBOE BXM/CLL: same. Quote the source line or withdraw the citation.
3. If both fail, state: "_DELTA_TARGET = 0.35 has no verified external reference" and stop. Do not substitute a new citation.

---

## ITEM 4 — Delta-aware snap (the real fix)

Targeting delta exactly at a `>= 0.35` gate is a coin flip. Your own output proves it: CLF put lands at |δ|=0.29, HAL put at |δ|=0.23. Both below the gate. Puts still will not trade.

Do NOT solve this by moving the target or the gate.

Implement exactly this:

1. Compute the continuous delta-target strike as you do now.
2. Identify BOTH neighboring grid strikes (floor and ceiling on the `_sinc` grid).
3. Compute BS delta at each of the two.
4. Select: among the two, choose the strike whose |δ| satisfies the live gate (>= 0.35); if both satisfy, choose the one closest to `_DELTA_TARGET`; if neither satisfies, return no candidate and log the reason.
5. Never select the arithmetically-nearest strike without evaluating delta.

Evidence required:
- Raw diff of the changed block.
- Test suite re-run, full raw output.
- New before/after table for CLF, HAL, AMGN, NEE, VRTX showing: chosen strike, |δ|, OTM%, and PASS/FAIL against the 0.35 gate — for calls AND puts.
- Explicit count of how many of the 10 legs now pass the gate.

Then STOP and report. No commit until Joel approves the table.

---

## ITEM 5 — TLA-42db2334 provenance

1. State whether TLA-42db2334 was issued by Joel via `tools/issue_tla.py` or generated by you.
2. Paste the raw record from `trading_logic_approvals.jsonl` for that ID.
3. If self-issued, annotate it `no_joel_approval: true` per existing procedure and report.

---

## ITEM 6 — Prod (unchanged, still blocking)

Prod is at `ad34726`. Joel clicks Publish. You then confirm `boot_commit_sha` via the preflight endpoint and paste raw output. Deadline Monday 09:40 ET.
