# AIEM — Full Wiring Plan (Priority Order)

Work through sections in order. Each has: the prompt to paste, the
verification tier required, and the pass condition. Don't skip ahead —
later items assume earlier ones are confirmed working.

Verification tiers used below:
- **[BEHAVIORAL]** — insert real test data, run it, confirm real output.
  No adversarial review needed.
- **[FULL]** — fresh-context adversarial review, then fact-check every
  claim the reviewer makes against raw command output before accepting it.
- **[RAW CHECK]** — a grep/query confirming the change exists as described.
  No test harness needed.

---

## 1. Fix `resolve_prediction()` — data integrity [BEHAVIORAL]

**Why first:** without this, `aiem_ml_predictions` outcomes may never get
filled in at all. Everything downstream (including future learning) is
worthless if this table's data isn't trustworthy.

**Prompt:**
```
resolve_prediction() in prediction_logger.py:85-105 is fully built (updates
outcome, return_pct, resolved_at in aiem_ml_predictions) but has zero call
sites anywhere in the repo. Find out:

1. What grading job SHOULD be calling this (compare to how
   aiem_prediction_outcomes gets graded via aiem_process.py:702-718 and
   main.py:31907-31923 — is there an equivalent job for aiem_ml_predictions
   that never got wired to call resolve_prediction()?)
2. Wire resolve_prediction() into that grading job so outcomes actually
   get filled in.
3. Show the exact diff, not a description.
```

**Verify:**
```
Insert a test row into aiem_ml_predictions with outcome IS NULL. Manually
trigger whatever job now calls resolve_prediction(). Query the row back
and show raw output: does outcome get filled in? Clean up the test row.
```
**Pass condition:** raw query shows the test row's outcome populated after the job runs, not NULL.

---

## 2. Fix paper trading regime multipliers [BEHAVIORAL]

**Why second:** cheap, isolated fix — paper trading fetches regime sizing
multipliers but never applies them. Quick win before the bigger wiring job.

**Prompt:**
```
premarket_open_trader.py:96 calls regime = rd.get_current_regime(db_url,
"SPY") and captures position_size_multiplier and confidence_multiplier in
the result, but never reads them back out to affect actual position sizing.

Fix this: apply position_size_multiplier to the position size calculation
and confidence_multiplier wherever confidence/conviction affects sizing in
this file. Show the exact diff.
```

**Verify:**
```
Simulate two regime states (e.g. multiplier=1.0 vs multiplier=0.5) and run
the sizing calculation for the same base trade under each. Print raw
position sizes for both. Confirm they differ proportionally to the
multiplier, not identical.
```
**Pass condition:** raw output shows position sizes actually scale with the multiplier.

---

## 3. Wire `regime_monitor_flags` into `combine_regime_votes()` [BEHAVIORAL]

**Why third:** the parameter already exists in `combine_regime_votes()` —
this just means passing it. Low-risk plumbing before the bigger conviction-stack wiring.

**Prompt:**
```
combine_regime_votes() in market_regime_overlay.py:168 accepts an optional
regime_monitor_flags parameter, but the only caller (regime_detector.py:152)
never passes it — so check_volatility_regime()'s z-score drift signal never
reaches the vote aggregator.

Also: run_all_regime_checks() in regime_monitor.py has zero external call
sites, so check_volatility_regime() never runs at all currently.

Fix both:
1. Wire a scheduler job (or call site) that actually invokes
   run_all_regime_checks() or check_volatility_regime() on a reasonable
   cadence.
2. Pass the resulting regime_monitor_flags into combine_regime_votes() at
   its call site in regime_detector.py.
Show the exact diff for both changes.
```

**Verify:**
```
Manually trigger the new scheduler job/call site. Query regime_flags table
raw output — confirm a new row was inserted. Then call
combine_regime_votes() and print raw output showing regime_monitor_flags
was non-empty and included in the vote computation (not silently ignored).
```
**Pass condition:** regime_flags table gets a new row when the check runs, and the vote output reflects it.

---

## 4. Wire regime output into `_run_five_layer_conviction` [FULL — money-adjacent]

**Why fourth, and why full verification:** this is the change that
actually makes regime awareness affect live picks. Gets the heaviest
scrutiny because it directly changes scoring on real decisions.

**Prompt:**
```
_run_five_layer_conviction (in main.py) currently has zero regime
awareness — none of combine_regime_votes()'s output (VIX, GARCH, z-score
drift, detect_regime()'s TRENDING/HIGH_VOL/CHOPPY classification) affects
any layer weight or conviction threshold.

Design and implement a connection: combine_regime_votes()'s output should
adjust conviction scoring for _run_five_layer_conviction — e.g. as a
multiplier on final conviction score, or as an input that shifts specific
layer weights (your choice, but document which approach and why).

Requirements:
- Must not require retraining anything — this is a rules-based adjustment,
  not a learned one (learning is being wired separately).
- Must be loggable — every conviction score calculated should record what
  regime was detected at that time, so it's auditable later.
- Show the exact diff.
```

**Verify [FULL — do all of this]:**

Step A — Behavioral test:
```
Run _run_five_layer_conviction on the same ticker/setup twice, once with
regime forced to a calm state and once forced to HIGH_VOL. Print raw
conviction scores for both. Confirm they differ in the expected direction
(document what "expected direction" means before running the test, then
compare against it).
```

Step B — Fresh-context adversarial review:
```
[New session, no memory of this task]
You are reviewing a code diff for correctness. Do not assume it is correct.
Find every way this regime-to-conviction wiring could fail or behave
unexpectedly.

Context: connecting market regime detection (VIX, GARCH vol forecast,
z-score drift) to a 9-layer stock conviction scoring system as a
[multiplier/weight adjustment — describe actual approach used].

Diff:
[paste raw diff]

Answer: what could make this fail silently? What edge cases aren't
handled? Does the code do what it claims, line by line? What would you
test that isn't already covered?
```

Step C — Fact-check every adversarial claim against raw output (grep,
run the actual function, whatever the claim requires) before accepting
or dismissing it. Do not take the adversarial review's word for anything
without raw evidence either way.

**Pass condition:** behavioral test shows real score differences by regime, AND every adversarial claim has been individually confirmed true or false against raw evidence.

---

## 5. Shadow-mode `propose_update()` wiring [BEHAVIORAL]

**Why fifth:** learning-loop visibility, zero live risk by design (logging
only, does not touch live weights) — same rigor tier as items 1-3.

**Prompt:**
```
online_learning.propose_update() (online_learning.py:91-161) is fully
built — gradient-descent weight updates with drift and held-out
performance gating — but has zero call sites anywhere in the repo.

Wire it in SHADOW MODE only:
1. On a schedule (or after N new graded outcomes from aiem_prediction_outcomes
   / summary.genuine), call propose_update() with the current layer weights
   and graded data.
2. Log the PROPOSED new weights to a new table — timestamp, old value,
   proposed new value, what graded data it was based on, and whether the
   regime was flagged as shifting during that window (per item 4's logging).
3. Do NOT apply the proposed weights to _run_five_layer_conviction. This
   is visibility only.
Show the exact diff and the new table schema.
```

**Verify:**
```
Manually trigger the new call site with a constructed set of graded
outcomes. Query the new proposal-log table, raw output. Confirm:
1. A row was inserted with sensible old/new weight values.
2. _run_five_layer_conviction's actual live weights are UNCHANGED before
   and after — print them both, confirm identical.
```
**Pass condition:** proposal logged with real values, live weights provably untouched.

---

## 6. Gated promotion mechanism [FULL — money-adjacent]

**Why sixth, and why last of the heavy ones:** this is the step that lets
proposed weight updates actually go live. Deliberately built last, and
deliberately requires manual approval, not automatic — do not build this
as automatic promotion. Also deliberately deferred until item 5 has
produced real proposal data to review — don't build this against
hypothetical data.

**Prompt (hold until item 5 has run for a while and produced real proposals):**
```
Build a manual-approval promotion step for the weight proposals logged in
[table from item 5]:

1. A review interface (CLI command or admin endpoint — your choice) that
   shows a pending proposal: old weights, new weights, the graded data
   basis, and regime context during that window.
2. Only on explicit manual approval does the proposal get applied to
   _run_five_layer_conviction's live weights.
3. Applying a proposal must be logged with a timestamp and the approver,
   the same way retrain_pipeline.py already gates the separate ML model
   promotion — reuse that pattern if applicable.
4. Never auto-apply. If no explicit approval exists, weights stay
   unchanged indefinitely.
Show the exact diff.
```

**Verify [FULL]:** same three-step treatment as item 4 — behavioral test (confirm a proposal only applies after explicit approval, never before), fresh-context adversarial review of the diff, fact-check every claim.

---

## 7. Broader-universe evaluation logging [RAW CHECK]

**Why last:** pure data-volume addition, doesn't touch scoring or trading
logic, lowest risk.

**Prompt:**
```
Keep the live/actionable recommendation list at its current size and
conviction threshold — do not change what surfaces as a trade
recommendation.

Separately, log a broader universe of scored tickers (target ~100/day) to
a new evaluation-only table, tagged with conviction tier, purely for
building a larger dataset to analyze layer performance. This data should
NOT feed into anything live-facing.
```

**Verify:**
```
Query the new eval table raw row count after one run. Confirm ~100 rows
logged, tagged with tier. Separately confirm the live-facing recommendation
count is unchanged (still the original threshold-gated count, not 100).
```
**Pass condition:** eval table has the broader dataset, live recommendations are unaffected.

---

## Backlog — not yet, revisit after the above is stable

- **True IV rank/percentile** — no existing scaffolding to hook into (real
  volatility percentile exists for realized vol via `check_volatility_regime()`,
  but not for IV). Build this once items 1-4 are wired and verified, not before.
- **Walk-forward validation specifically for `propose_update()`** — worth
  revisiting once shadow mode (item 5) has produced enough real proposals
  to know if this is actually needed.
