# Directive: Verify Against Actual Replit Deployment Migration Panel Before Confirming

## Context

Your prior evidence chain ran `drizzle-kit generate` against the **local dev DB** and confirmed via fresh grep that none of the 6 flagged AIEM tables (`aiem_health_log`, `aiem_ml_predictions`, `aiem_prediction_outcomes`, `aiem_predictions`, `aiem_probability_engine_live_queries`, `aiem_research_hypotheses`) appear in any DROP TABLE statement, and that the migration SQL contains only 4 CREATE TABLE statements (`questions`, `answers`, `sessions`, `affiliates`).

This is good evidence, but it is a **proxy**, not the actual artifact. The Replit deployment UI's migration SQL panel — the screen that only renders when a publish is initiated from the browser — may not be generated by the identical mechanism as a locally-run `drizzle-kit generate`. Do not treat the dev-DB simulation as sufficient grounds to click confirm.

## Task

1. Initiate the publish flow from the browser far enough to render the actual migration SQL panel in the Replit deployment UI. Do **not** click confirm.
2. Copy the **exact, complete raw SQL** shown in that panel — not a paraphrase, not a re-run of `drizzle-kit generate`.
3. Save that raw SQL to a file and run `grep -ic "drop table"` on it for each of the 6 flagged tables individually, same as Step 5 in your prior evidence chain.
4. Paste:
   - The raw SQL panel content in full (or the relevant DROP/CREATE statements if the full panel is very long — but state if you are truncating and why).
   - The grep output for each of the 6 tables against this actual panel content.
   - A clear statement of whether this matches or diverges from your prior dev-DB simulation.
5. Still do **not** click confirm. Wait for my explicit go-ahead after I review this evidence.

## Standing Requirements (apply to this directive)

**Verification block:** No task is complete based on description alone. Provide (1) raw terminal output of actual execution, including raw grep output for any claim about code/data — never paraphrase; (2) exact before/after file diff if applicable; (3) sha256 hash before/after for any changed files; (4) raw SQL query + full result set. Narrative claims of "confirmed" or "verified" without pasted raw evidence are rejected. If you cannot produce evidence for any part of this, say so explicitly.

**Data Immutability Rule:** You may never delete, truncate, drop, or overwrite any row, table, or file — including anything you created or labeled "test"/"synthetic"/"cleanup" — without my explicit prior approval in this session. State what you want to delete and why, wait for approval, then log it. Any unapproved deletion is a protocol violation to report, not self-correct.

**Evidence Chain Protocol:** Run all evidence-producing commands through `verified_run.sh` and validate with `verify_chain.sh` before declaring this directive complete. Paste that script's output.
