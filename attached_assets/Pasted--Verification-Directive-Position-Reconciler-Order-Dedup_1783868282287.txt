# Verification Directive — Position Reconciler, Order Dedup, and Wiring Sweep Evidence

**Date:** July 12, 2026
**Context:** Follow-up to Phase 10/12/14/15 wiring sweep and E2E live-verification trace.

---

## STANDING VERIFICATION BLOCK (applies to every item below)

No task or answer is accepted based on description alone. For every claim below, provide:

1. **Raw terminal output** of the actual command/execution used to check it — not a paraphrase.
2. **Exact before/after file diff** for any file touched.
3. **SHA256 hash (before and after)** for any file changed.
4. **Raw SQL query text + full unedited result set** for any DB-backed claim.

If any of these four cannot be produced for a given item, you must explicitly say so — do not substitute a narrative description ("this is fixed" / "this is wired" / "this passes") in place of pasted evidence.

## STANDING DATA IMMUTABILITY RULE

You may not delete, truncate, drop, or overwrite any row, table, or file — including data you created yourself or label "test," "synthetic," or "cleanup" — without my explicit prior approval in this session. State what you want to delete and why, wait for approval, then log it. Any unapproved deletion is a protocol violation to report, not self-correct.

---

## 1. Position Reconciler (Item A6)

**Claim under review:** `reconcile_positions()` is absent from the paper-execute gate path — flagged as NOT WIRED, "known gap."

Answer with evidence:

1. Paste the raw `grep -n` (or equivalent) output searching the entire codebase for every call site of `reconcile_positions`. Show file paths and line numbers.
2. Paste the raw `grep -n` output for the paper-execute gate function itself (the function that runs before/during `_aiem_paper_execute_today()` or equivalent) — show its full body, not a summary.
3. Confirm with evidence: is `reconcile_positions()` defined anywhere in the codebase but simply never called (dead code), or does it not exist at all? Paste the function definition if it exists.
4. State explicitly: what specific position-drift scenario does the ABSENCE of this reconciler leave unguarded right now? (e.g., broker-reported position vs. internal ledger diverging silently)
5. Do not fix or wire anything yet. This is a diagnostic-only step. Wait for my explicit decision (wire it vs. formally retire the spec requirement) before touching code.

---

## 2. Order Dedup (Item A7)

**Claim under review:** No application-level dedup pre-check or governance event exists — dedup relies on DB constraint only.

Answer with evidence:

1. Paste the raw SQL (e.g., `\d+ tablename` in psql, or `SELECT conname, pg_get_constraintdef(oid) FROM pg_constraint WHERE conrelid = 'tablename'::regclass;`) showing the exact DB constraint currently providing dedup protection. Include the full result set.
2. Paste the raw `grep -n` output for any application-level pre-check code that runs before an order/trade insert — show whether one exists or truly does not.
3. Paste the raw `grep -n` output for any governance event emission tied to a duplicate-order attempt (search for however governance events are emitted elsewhere in the codebase, e.g. `emit_governance_event`, `_log_governance`, etc., and confirm none fire on constraint violation).
4. Run (or show you cannot safely run without approval) a controlled test: attempt to insert a duplicate order in a test-isolated context and paste the raw output/error showing what actually happens today — DB rejection only, silent, logged, or governance-visible.
5. State explicitly what a duplicate-order attempt looks like from D3's perspective right now: does D3 see it at all, or is it invisible to governance?
6. Do not fix or wire anything yet. Wait for my explicit sign-off before implementing.

---

## 3. Wiring Sweep Re-Verification (10/10 modules, 27/27 tools)

**Claim under review:** "10/10 modules WIRED, 0 gaps" and "27/27 AI tools have real dispatch-map entries (0 dispatch gaps)."

Provide the underlying evidence, not just the summary table:

1. Paste the raw `grep`/script output (whatever produced the 10/10 and 27/27 counts) in full, unedited — the actual command run and its actual output, not the formatted table shown in chat.
2. For each of the 27 tools, confirm the dispatch-map entry exists by showing the raw grep hit (file:line) for its entry point.
3. If this was generated by a script/audit tool rather than manual grep, paste the script's source and its raw stdout from the actual run (with timestamp) — not a re-description of what it "would" show.

---

## 4. Stage 18/21/23 audit_trace_id Dependency

**Claim under review:** Conditional stages silently skip their D2 stage log when `audit_trace_id IS NULL`.

1. Paste the raw SQL query + full result set showing the count of trades where `audit_trace_id IS NULL`, and the date range these span.
2. Confirm with evidence whether this is truly "expected behavior" or a coverage gap: paste the raw SQL showing how many of those NULL-trace trades are still open/active today (i.e., still economically relevant, not just historical).
3. State plainly: is a backfill of `audit_trace_id` for pre-existing trades possible, and what would it require? Do not implement — diagnostic only.

---

## 5. Stale Memory Pointer (directive4-test-mode.md)

1. Confirm with a raw `ls -la` or `find` output whether `directive4-test-mode.md` (or `D3_directive4_test_mode_isolation.md`) exists on disk right now, full path included.
2. If it does not exist, state where the memory/index pointer currently referencing it lives, and confirm you have NOT deleted or altered anything to "clean this up" without approval.

---

## Closing Instruction

Do not mark any of the above as resolved, fixed, wired, or passing without the specific evidence type requested for that item. If you hit a limitation (can't run a query, can't safely test dedup without risking live data, etc.), say so explicitly and propose a test-isolated approach — do not work around it silently.
