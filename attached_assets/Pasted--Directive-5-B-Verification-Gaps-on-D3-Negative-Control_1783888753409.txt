# Directive 5-B: Verification Gaps on D3 Negative-Control Test PASS Claim

Your Directive 5 Option 2 report claims PASS. Before this is accepted, resolve the following four items. Narrative explanation of any item is not acceptable — the raw artifact is required in every case.

---

## 1. `is_test_record=FALSE` discrepancy

The decision row on record (`GDEC_G0_f8fd9df1bfa348bcb55665d964d0adad`) shows `is_test_record=FALSE`. If this field exists to flag test-generated decisions, why is a forced negative-control BLOCK showing FALSE?

- Paste the exact code path that sets `is_test_record`, showing where/how it's assigned.
- State explicitly: was this specific decision row generated by the test harness, or is it a real/production decision that happens to have BLOCK/STATE_PAUSED? These are not the same thing — state which one this is.

## 2. Origin of the BLOCK verdict

Did D3's actual live code path (`d3_startup()` → `subscribe_to_bus()` → `_on_bus_stage_event()`) compute this BLOCK decision from a real STATE_PAUSED condition that was induced? Or was a row manually inserted/crafted into `d3_governance_decisions` with `decision=BLOCK` purely to test downstream consumption?

- Paste the exact function call sequence and raw log/stdout output showing the verdict being computed — not just the resulting row.
- If it was manually inserted rather than organically computed, state this explicitly. This changes what has actually been proven.

## 3. Full subprocess test — do not substitute the dry-run

The claim that "the dry-run is the complete proof" because the >180s import time bypasses deferred-init is a narrative interpretation, not evidence, and is rejected per standing protocol.

- Either produce the actual full subprocess test run (raw terminal output, timestamps, exit status), **or**
- Explicitly state that this evidence item cannot be produced, and why — per the standing verification block requirement to declare inability rather than substitute a workaround.

## 4. Raw evidence, not summary tables

The schema-delta "Value / Result" tables in the report are a self-generated summary. Provide:

- Raw SQL: the actual `SELECT` queries run against `d3_test_isolation.*` and `public.*` tables (before/after row counts), with full result sets pasted — not a rendered table.
- The raw `aiem_paper_execution_log` row and confirmation that the `aiem_paper_trades` INSERT was never reached (paste the actual query result, not the code-reading claim).
- sha256 hashes before/after for any file touched in this directive.

---

**Do not mark VERIFIED SHADOW as achieved until all four items are answered with pasted raw evidence.** If any item cannot be produced, state this directly rather than reframing around it.
