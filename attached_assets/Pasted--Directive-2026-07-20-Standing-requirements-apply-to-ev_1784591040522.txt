# Directive — 2026-07-20

## Standing requirements (apply to every item below)
- Raw command output only — no narrative claims, no paraphrasing
- Raw grep/sed for any code-location or behavior claim
- sha256 before/after for any changed file
- Full SQL query + full result set for any DB claim
- git diff HEAD --stat (or "no changes") to confirm no unintended writes
- NO deletion/archival of any file without explicit approval in this session — state what you want to delete and why, then wait
- If evidence can't be produced, say so in one line — no padding

---

## ITEM 1 — Datetime/day-of-week integrity (P0 — must be 100% before anything else is trusted)

Run live, do not read from a prior DB row or log:
1. `date` (raw shell command, UTC)
2. `python3 -c "from datetime import datetime; import pytz; et=pytz.timezone('America/New_York'); n=datetime.now(et); print(n.strftime('%A %Y-%m-%d %H:%M:%S %Z'), 'weekday()=', n.weekday())"`
3. `SELECT NOW(), NOW() AT TIME ZONE 'America/New_York', EXTRACT(DOW FROM NOW() AT TIME ZONE 'America/New_York');` — raw SQL output
4. Raw grep of every place `.weekday()` or day-of-week logic is used in `aiem_options_scheduler.py` and `aiem_paper_recovery.py` (line numbers + context)
5. State plainly: does the live output agree that 2026-07-20 is a Monday and 2026-07-21 is a Tuesday? If any of steps 1–3 disagree with each other or with reality, report that as a bug — do not reconcile it narratively.

This gates everything else. If runtime day-of-week is wrong, the weekend skip guard and the Monday/Tuesday CronTrigger unblock checks are both unreliable until fixed.

---

## ITEM 2 — verified_run.sh duplicate copies (consolidate)

1. Filesystem-wide `find / -name verified_run.sh 2>/dev/null` (or repo-scoped equivalent) — raw output, every path + sha256 of each
2. For each copy: `git log --follow -p -- <path>` raw output — full commit history, no summarizing
3. Propose ONE canonical path with reasoning (which one is actually referenced by the live scheduler/DPL code — raw grep showing the reference)
4. List every file that currently references any of the non-canonical copies (raw grep)
5. DO NOT archive or delete any copy yet — report findings and wait for explicit go-ahead on which paths to remove

---

## ITEM 3 — Dirty tree cleanup (before trusting any PASS count)

1. `git status --porcelain` raw output — current dirty file list
2. For each of the 7 previously-flagged dirty files, state: commit it, or discard it — and why (one line each)
3. After resolving, re-run `check_clean_tree` and paste raw output — confirm RESULT=CLEAN
4. Re-run the full check suite against the now-clean tree and paste the raw pass/fail summary line — do not reuse the prior 195-PASS number, it was measured against a dirty tree
5. `git diff HEAD --stat` after cleanup to confirm no unintended changes

---

## Order of execution
Item 1 first — halt and report if it fails before touching 2 or 3. Items 2 and 3 can run in parallel after Item 1 passes.
