# DIRECTIVE: Deployment Data-Loss Near-Miss — Stop, Verify, Regenerate

## Context

During today's deployment attempt, the Replit publishing UI flagged that the pending migration would **delete 6 production AIEM tables**, including live data:

- `aiem_health_log` — 256 items
- `aiem_ml_predictions` — 229 items
- `aiem_prediction_outcomes` — 5 items
- `aiem_predictions` — 5 items
- `aiem_probability_engine_live_queries` — 18 items
- `aiem_research_hypotheses` — 44 items

You (the agent) told me this warning was cosmetic/informational, and that the actual migration only contained a `CREATE TABLE aiem_intraday_grid_state` statement with no drops — based on viewing a partial scroll of the migration output. You then separately acknowledged that the `tablesFilter` fix you applied to `drizzle.config.ts` had **not taken effect** because this deployment was already in progress when you made the change, and explicitly told me not to click Submit/Continue because it would still delete data.

These two statements contradict each other. I did not deploy. This directive requires you to resolve the contradiction with evidence before we go anywhere near a deploy button again.

## What went wrong (your task: confirm or correct this)

1. Replit's deployment UI does not fabricate table names and exact row counts. That warning is generated by parsing the actual migration diff. If it named 6 specific tables with specific row counts, the migration SQL almost certainly contains `DROP TABLE` statements for those tables — not just the single `CREATE TABLE` line you quoted to me.
2. Drizzle generates migrations by diffing its schema files against the live database. If those 6 tables exist in production but are not currently declared in Drizzle's schema files, Drizzle will treat them as "should not exist" and write drops for them. This is standard Drizzle behavior — it is exactly the failure mode `tablesFilter` was supposed to prevent.
3. Your own message confirms the `tablesFilter` fix did not apply to this in-flight deployment.

**Conclusion until proven otherwise: this deployment, if submitted, would have dropped 6 production tables containing 557 combined rows of live AIEM data.** Treat this as a confirmed near-miss, not a false alarm, unless you can produce evidence to the contrary per the verification block below.

## Required actions

1. **Do not resume or reuse the in-flight deployment.** Cancel it fully if any part of it is still pending.
2. **Confirm the `tablesFilter` is actually active** in the config Replit's deployment system reads (not just present in `drizzle.config.ts` — confirm which file/mechanism Replit's migration step actually consumes).
3. **Regenerate the migration from scratch** after the filter is confirmed active.
4. **Paste the complete migration SQL output** — every statement, not a summary or a single excerpted line — before I approve any deploy action. If `DROP TABLE` statements still appear for any of the 6 tables listed above, stop and report back; do not attempt to "explain away" the warning again.
5. Explain, with evidence, why the warning appeared this time (i.e., which schema files were missing declarations for those 6 tables, if that's the actual cause) so this doesn't recur on the next deploy.

## Verification block (required)

No task is complete based on description alone. You must provide:
1. Raw terminal output of actual execution, including raw grep/cat output for any claim about migration contents, config file contents, or which config file Replit's deploy step reads — never paraphrase what a command would show.
2. Exact before/after diff of any config file changes.
3. sha256 hash before/after for any changed files.
4. The full raw migration SQL, not a description of it.

Narrative claims of "fixed," "confirmed," or "safe" without pasted raw evidence are rejected. If you cannot produce the evidence, say so explicitly.

## Data Immutability Rule (required)

You may never delete, truncate, drop, or overwrite any row, table, or file — including data you created yourself or label "test"/"synthetic"/"cleanup" — without my explicit prior approval in that session. You must state what you want to delete and why, wait for approval, then log it. Any unapproved deletion is a protocol violation to report, not self-correct. A deployment that silently drops these 6 tables would fall under this rule — treat the near-miss itself as reportable.

## Evidence Chain Protocol (required)

Run all evidence-producing commands through `verified_run.sh` instead of directly, and validate the log with `verify_chain.sh` before declaring this directive complete. Paste that script's output. This proves internal log consistency only, not that commands ran against the real production DB — I will spot-check independently.
