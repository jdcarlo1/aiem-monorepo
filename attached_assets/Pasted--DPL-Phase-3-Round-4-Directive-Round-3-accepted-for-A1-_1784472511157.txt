# DPL Phase 3 — Round 4 Directive

Round 3 accepted for A1, F1, F2, F4, F5, F6, F7. Do not proceed past R4.1 until it passes.

## R4.1 — verified_run.sh: run before canonical
Canonical sha256 will NOT be recorded for a script that has never executed.
1. Run the patched script (`237a7e9ee64eca92…`) once. Show raw SEQ, EXIT, TS_END, sha256(log), full C01–C19 summary.
2. Confirm SEQ=3. If not 3, stop and report.
3. Restart the VM (or flush /tmp), run again, show SEQ=4. This is the durability proof — the patch is unproven without it.

## R4.2 — grep -m1 defect
`LAST_SEQ=$(grep -m1 "^SEQ=" "${LOG_FILE}")` returns the FIRST match, not the last. Your comment says "last log line."
- Show raw: is LOG_FILE truncated (`>`) or appended (`>>`) each run? Show the redirect line.
- If appended, SEQ freezes at the oldest value. Fix with `grep "^SEQ=" | tail -1` or equivalent, and re-run R4.1.

## R4.3 — replay_decision() has no production call site
`grep -rn "replay_decision"` returned no output outside the definition and verifier. Drift detection therefore never runs in production — only on manual verifier invocation.
- State plainly whether this was intended.
- Propose (do not implement) where replay_decision() should be invoked in the live path, and what happens on CODE_DRIFT: alert, halt, or log-only.
- Awaiting approval before any change to scheduler or pipeline.

## R4.4 — provenance of the five is_test_record=FALSE rows
Round 2 stated Criterion 1 was blocked because no real production rows existed before 2026-07-20 09:45 ET. Five now exist.
- Raw query: decision_id, created_at, alert_id for all five, joined to oe_decision_audit.
- State for each whether it originated from a live scheduler decision or from backfill/manual insert.
- If backfill: Criterion 1 is still blocked and R6 stays open. Say so.

## R4.5 — permanently unreplayable rows
`ee74327806` (old source-only hash) and `64d956c7ee` (D12=0.99, mutation window) both raise CODE_DRIFT and cannot be replayed.
- Confirm both are retained, not deleted.
- State the DPL's declared position: are these counted as failures against Criterion 1, or excluded with documented cause?

## R4.6 — SEQ chain discontinuity
SEQ ran 3 → 2 across Round 3. The counter is not a chain and cannot order the historical record.
- Add an explicit statement to the run log or README that SEQ is per-VM-lifetime and non-continuous prior to the R4.1 durability proof.

## Standing requirements (every reply)
- Raw command output, not prose summaries.
- sha256 of verified_run.sh and verify_chain.sh cross-checked against canonical, full untruncated.
- `git diff HEAD --stat` and `git status --porcelain` captured AFTER all work in the reply, not before.
- No mutation of any live production file. Mutation proofs use `_test_mode=True` or an offline copy, scheduler stopped. No exceptions, no self-authorized restores.
